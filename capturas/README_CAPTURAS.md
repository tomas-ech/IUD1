# Evidencia de capturas

El Dashboard fue verificado visualmente en el navegador integrado durante la ejecución:

- Carga correcta de `http://127.0.0.1:8050`.
- Estado visible: `API conectada correctamente`.
- Filtros visibles: tipo de sensor, ubicación y slider de registros.
- Tarjetas visibles: mínimo, máximo y promedio.
- Gráfico visible: valores de sensores en tiempo real.
- Prueba End-to-End confirmada: después de crear `Sensor Integracion 1` por API, el promedio cambió a `46.85` sin recargar la página.

Nota operativa: el entorno bloqueó la escritura directa de archivos PNG desde el navegador integrado. La evidencia visual fue observada durante la verificación y el resultado quedó registrado en `BITACORA.md`.
